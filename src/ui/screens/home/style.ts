import styled from 'styled-components'

import { Sidebar as SidebarStyled } from 'ui/components/sidebar'

export const Wrapper = styled.div`
  display: flex;
`

export const Sidebar = styled(SidebarStyled)``
